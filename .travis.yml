language: c

compiler:
  - gcc
  - clang

before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -qq gsl-bin libgsl0-dev

install: `gsl-config --prefix' && make -j4 && make install
	
script: make tests


